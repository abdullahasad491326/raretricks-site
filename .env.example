# Copy this to .env and fill values, then DO NOT commit .env
PORT=3000

# If the external API requires a token/key add it here (example)
#API_KEY=your_api_key_here

# Optional: admin password to access protected endpoints (if you enable)
#ADMIN_PASS=change-me
